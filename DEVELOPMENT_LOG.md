# 开发日志

记录已完成的功能、验证结果与待办。日期使用北京时间（Asia/Shanghai）。

## 2026-09-17 · 多管理 AK 与生成 AK 绑定

- 根据用户提供的 `management-api.md` 增加独立管理 AK 配置，多配置命名/单选启用/只读身份验证，隐藏文件与生成 AK 分离，凭据不回显且不允许混填。
- 每个生成 AK 指定绑定管理 AK，多个生成 AK 可以共用；绑定查询不受当前启用管理 AK 切换影响，未绑定不回退到其他凭据。删除或跨组织替换绑定中的管理 AK 前要求改绑。
- 直接管理 API 查询组织总费用、按模型费用与按生成 Key 的近似分摊费用，支持周期或 UTC 日期范围，提示未定价模型导致的汇总不完整。文档未提供单条请求费用接口，保留项目真实账单导入机制。
- 新增凭据分离、多个管理 AK、绑定持久化、删除保护、错误配置不落盘、绑定查询和缺少绑定不回退的测试，桌面回归覆盖创建管理 AK、生成 AK 绑定、切换管理 AK 后仍使用绑定组织查询。

## 2026-09-17 · 生成费用与历史汇总

- 增加当前项目历史汇总、日期/类型/状态/关键词筛选、逐条费用与模型开销统计，包含隐藏结果和移除卡片后的记录。按币种统计已知费用，未知费用独立显示。
- 服务响应费用在转存前保存；终态视频可以使用原 Key、原接口版本只读补查费用，不重新提交生成。保留原有账单费用，缺失值不覆盖为零。
- 核对已登录 service-inference 控制台请求与 NDJSON：`/user/logs`、`/user/logs/record`、`/user/logs/summary`、`/user/logs/export` 通过网页登录会话访问；当前生成 Key 测试返回 403，未保存网页登录凭据，暂未自动同步日志。
- 增加控制台 NDJSON 导入，以视频 taskId / 新图片请求 ID 和模型精确回填当前项目真实 USD 账单；重复导入幂等，冲突账单拒绝写入，不通过时间猜测关联。
- 验证：223 项 Python 测试通过；Electron 云端工作台回归通过，验证重复账单、文件导入、类型筛选、汇总金额与界面截图。仅读取服务商日志及模型资料，未发起实际付费生成。

## 2026-09-13 · service-inference 云端生成

- 新增独立 API Key 设置、清除与只读连接验证；密钥位于有效数据目录下的隐藏文件 `.service-inference.json`，使用 0600 文件权限，接口不回显密钥，生成记录仅保留允许的参数字段。
- 接入 4 个 Seedream 图片模型、豆包 / Dreamina 各 4 个 Seedance 视频模型，以及云端 MiniMax H3。卡片根据模型显示分辨率、画幅、时长、音频和公网参考 URL 输入；保留 ComfyUI 工作流。
- 图片使用同步 API 后台执行；视频持久化远端任务 ID，后台轮询并支持应用重启续查。付费提交不自动重试。完成的文件保存在 `generated/`，通过归属校验提供预览及视频 Range 播放。
- 云端任务不参与 ComfyUI 取消与排序，也不阻止 ComfyUI 地址调整。密钥更换在有活跃云端任务时阻止。暂不支持本地素材公网托管、组图、流式图片和云端取消。
- 验证：107 项 pytest 通过；首次启动与原有画布 smoke 通过；新增隔离云端 UI smoke 通过，覆盖设置、四类模型的模拟生成、视频播放及 Range、保存重开、窄窗口。截图检查设置和云端参数显示。真实 API Key 仅做只读鉴权验证，未进行付费生成实测。

## 2026-09-13 · macOS 本地源码部署

- 新增可双击的 `Start_Dev.command`，自动切换到仓库并运行源码桌面端；README 补充 macOS 环境准备说明。
- 使用独立 Python 3.12 环境及 Homebrew PostgreSQL 18.6，项目 `runtime/pgsql/bin` 链接到 PostgreSQL 二进制；运行数据保存在 `.local/`。
- 修正首次启动检查：无 `src` 的空预览图片不参与品牌图片加载判定。
- 已通过依赖一致性检查、三数据库初始化、Electron 首次账号设置页面及窄窗口检查、项目创建、画布卡片参数切换、八方向缩放、自动保存与重开检查。UI 验证使用 `.test-data/` 隔离数据。
- 已保存局域网 ComfyUI 配置，应用连接检测返回 0.35.0；未提交 GPU 生成任务，模型实际生成能力尚未验证。

## 2026-09-12 · 首次开发汇总

### 提交历史说明

仓库起点为 `28cdc56`（Initial commit）。下面各阶段在开发时尚未逐步提交；本条日志是依据本次实际开发过程补录，随当前完整源码一起提交，不代表存在对应的历史阶段提交。此前也未推送这些改动。

### 阶段一：Electron、Tornado 与 PostgreSQL 基础

- 建立 Electron 桌面入口，启动独立 Python/Tornado 子进程，通过标准输出取得本机 HTTP 端口；退出时通知后端关闭它启动的 PostgreSQL。
- 配置 PostgreSQL 18.6 本地运行组件，默认监听 `127.0.0.1:55432`，使用 SCRAM 密码认证，支持切换至外部实例。
- 初始化 `hotpoor_director`、`hotpoor_director1`、`hotpoor_director2` 三个数据库。
- 两个实体库各仅一张用户表 `entities`：`block_id` 为无连字符的 32 位十六进制 UUID，`body` 为 JSONB，`createtime`、`updatetime` 为 BIGINT 毫秒时间戳；数据库触发器维护更新时间。
- 主库 `index_login` 保留 `login`、`user_id`、`createtime`、`updatetime` 四个字段。密码哈希放在 `auth_credentials`，登录会话放在 `auth_sessions`。
- 实现首次创建账号、账号密码登录、当前用户查询、退出登录、XSRF 校验与登录尝试限流。密码采用 Argon2id 哈希，服务端仅保存会话令牌的 SHA-256。
- 首次创建账号接口受桌面进程生成的启动凭据保护，已有账号后关闭首次创建入口。
- 本地配置使用隐藏目录与隐藏文件，Windows 限制当前用户访问。真实账号配置、数据库数据、个人访问令牌与构建产物不纳入 Git。
- 增加 PyInstaller 独立后端与 Electron Builder 分发配置，产出过 Windows 解包版，用于验证打包运行。

处理的问题：

- 本机旧 C++ 运行库导致 PostgreSQL 初始化崩溃，给项目内 PostgreSQL 配置应用本地运行库。
- Windows 限权子进程读取初始化文件失败，调整应用数据目录的当前用户权限。
- `pg_ctl` 子进程继承管道导致等待 EOF，改用临时文件收集命令输出。
- 本机首次数据库落盘较慢，延长安全启动与关闭等待时间。

验证：

- 源码后端：6 项真实 PostgreSQL 集成测试通过。
- PyInstaller 独立后端：同样 6 项集成测试通过。
- 覆盖实体表字段、UUID 校验、JSONB 更新、毫秒时间戳、账号唯一性、密码哈希、首次账号保护、XSRF、登录与会话注销、限流、24 次并发写入和重复初始化。
- Electron 首次账号界面检查通过，Python 依赖检查通过。

### 阶段二：品牌 Logo 与黑白界面

- 保存用户提供的原始 Logo，生成多尺寸 Windows ICO、应用 PNG、网页 favicon 和触屏图标。
- 将 Logo 配置到 Electron 窗口、Windows 应用资源、安装器配置和页面。
- 页面调整为黑白灰配色，品牌区与登录区左右布局，使用细线、留白、衬线标题和电影制片文案；窄窗口切换为纵向布局。
- 普通登录、首次账号创建、登录后欢迎界面统一使用该品牌样式。

验证：

- Electron 宽窗口、640px 窄窗口截图检查通过，无横向溢出。
- Logo 图片与 `/favicon.ico` 加载检查通过。
- Windows 程序内嵌的 7 个 ICO 图像资源与源图标匹配。

### 阶段三：改为源码开发启动（当前默认流程）

- 用户要求日常直接运行源码，不再为每次修改打包应用 EXE。
- 新增 `Start_Dev.bat` 和 `npm run dev`，直接运行 Electron 与 `.venv` 中的 Python/Tornado 源码。
- 监听 `backend/web` 文件变化，保存页面 HTML、CSS、JS 或图片后自动刷新。
- 开发模式关闭 Tornado 模板和静态资源哈希缓存，F12 切换开发者工具。
- Python 与 Electron 主进程修改后，通过关闭并重新启动生效。
- 开发目录尚无配置时自动沿用此前桌面版的账号数据库；仍可通过 `DIRECTOR_DATA_DIR` 显式选择数据目录。
- 保留分发构建脚本供以后使用；当前日常启动不执行构建。

验证：

- JavaScript 语法检查与 Python CLI 参数检查通过。
- 确认启动的后端命令为 `python -m backend serve ... --dev`。
- 确认沿用已有用户数据目录，本地页面 HTTP 返回 200。
- 开发模式变更后未重新运行整个数据库集成测试套件；上面的两组 6 项结果对应阶段一完成时的版本。

### 当前限制与后续工作

- 导演项目、镜头、素材和编辑功能尚未实现；登录后的工作空间目前为占位界面。
- 三个数据库是同一实例内的独立数据库，不等同于主从副本；读写分离、复制和跨库事务方案尚未实现。
- Python/Electron 主进程暂不自动重启，需要手动重开。
- 当前验证平台为 Windows x64；其他平台需准备对应 PostgreSQL 运行组件并单独构建验证。
- Windows 公开分发仍需配置代码签名并检查运行组件再分发许可。

## 后续记录约定

每完成一个可验证阶段，补充日期、改动、验证、已知限制，并及时提交；按用户当前要求推送到 `main`。阶段历史以实际 Git 提交为准，不虚构补录提交。推送失败时明确记录失败原因，不把本地提交描述为已同步。

## 2026-09-12 · Dashboard、无限画布与本地生成

本阶段在首次源码汇总尚未提交时继续开发；随本次完整快照提交，不将此前工作伪装成逐步历史提交。

- 新增按账号隔离的项目 Dashboard：主标题、副标题、描述、多封面、服务端 32 位 UUID、毫秒创建/更新时间；可搜索与编辑设置。
- director1 的 entities 保存项目和上传资源元信息，director2 的 entities 保存生成历史，维持两个实体库各仅一张表的约定。
- 新增无限画布平移/缩放、添加图片/视频卡片、标题拖动、四边四角缩放和视角适配。
- 图片 tab 为文生图/图生图/参考图，视频 tab 为文生视频/图生视频/多元素参考；每个 tab 独立保存草稿。
- 卡片顶部结果预览、可调 pin 比较位、最新在左的横向历史、历史参数/模型/耗时/token 信息及复用参数。
- 700ms 自动保存，串行写入、revision 冲突检查、断线重试与未保存离开提醒，支持导出 JSON 草稿。
- 接入本机 ComfyUI：Z Image Turbo 文生图与原图 VAE 重绘，MiniMax H3 fl2va 文生视频与首尾帧图生视频。参考模式缺少对应模型时清晰禁用，未伪装支持。生成请求使用 UUID 去重。
- 本地模型未返回 token 统计，保存 null 并展示“未提供”，不伪造消耗。
- 使用后来提供的透明 Logo 原图替换带边框截图，保留黑色原版并派生白色反白 PNG/ICO，页面使用反白版。

验证记录：

- 9 项真实 PostgreSQL 集成测试通过，新增项目隔离、封面访问、画布持久化、revision 冲突、生成历史隔离等检查；初次新增测试的 HTTP 客户端重复进入上下文问题已修复。
- Electron UI 检查通过：项目创建、两个类型卡片、独立 tab 草稿、八个方向实际鼠标缩放、自动保存及重新打开、640px 窄窗口无页面横向溢出。
- 两种模型的生成工作流通过本机 ComfyUI 参数校验；Z Image 文生图实际生成成功。
- H3 文生视频实际生成成功（256×256 小尺寸测试，输出 MP4）。完整生成 API 图生图验证通过：参考图片上传、入队、请求 UUID 去重、历史持久化、私有输出与 token 空值展示。
- 最终源码测试再次通过：9 passed；未执行 Electron EXE 打包。

当前限制：参考条件模型未安装/接入；ComfyUI 输出仍由该服务保存和提供；超时提交需人工核实队列；尚无多人实时协作、历史分页、撤销重做和跨库事务。此前“登录后工作空间为占位界面”的限制已被本阶段替代。

补充界面验证：多封面上传、真实生成结果预览、历史点击、pin 对比项保存及 token 未提供提示均通过；修正预览图片被容器裁切的问题。

## 2026-09-12 · 独立图片放大与全屏预览

- 点击生成大图、历史缩略图或 Pin 对比图片，打开独立于画布变换的图片预览层。
- 支持滚轮/按钮缩放、拖动查看、100% 原尺寸、适应窗口、双击切换尺寸、历史上一张/下一张及键盘左右键。
- 支持系统全屏、退出全屏和 Esc 关闭；模态层限制焦点，关闭后恢复焦点。图片加载失败显示明确提示。
- 保留历史缩略图选择与详情更新行为；图片预览操作不修改画布位置与尺寸。
- Electron 实际交互验证通过：三个入口、125% 缩放、鼠标拖动、进入/退出系统全屏、Esc 关闭。截图检查通过。无后端/数据库变更，未重新执行数据库测试，也未打包 EXE。

## 2026-09-12 · 生成信息吸顶

- 图片和视频卡片的“生成信息”在向下滚动时固定于卡片内容顶部。
- 展开高度最多 240px，长详情独立滚动，标题保持可见；折叠状态保留吸顶入口。
- Electron 实际滚动检查通过：折叠/展开均保持顶部位置，展开高度为 240px；截图确认参数区仍可操作。仅修改样式，无数据库变更。

## 2026-09-12 · 真实生成进度与历史图片切换

- 为图片和视频生成增加黑白进度条。后端按登录用户连接 ComfyUI WebSocket，读取实际采样步数；排队、模型加载和输出处理显示不定进度，百分比只表示采样进度。
- 活跃任务每秒更新；进度变化只更新进度条，保留当前图片、视频播放和详情滚动位置。后台任务生成时仍可切换查看旧图片。
- 进度缓存限制为 1000 项，不写入数据库；连接中断后清理旧进度并在下一次查询重连，应用退出时关闭连接。
- 历史缩略图点击仅切换当前图片和生成信息。当前大图、Pin 图片继续支持独立放大/全屏，预览内仍可切换历史图片。
- 两项进度单元测试通过，验证用户隔离、采样节点切换和缓存上限。本机 ComfyUI 实际图生图测试通过：观察到第 2/4 采样步并成功取得私有图片输出；重复请求去重与 token 空值检查通过。
- 回归验证：9 项真实 PostgreSQL 集成测试通过；Electron 历史点击不打开预览、大图/Pin 放大、缩放、拖动、进入/退出全屏、Esc、画布缩放与自动保存检查通过。隐藏窗口输入时序稳定后重跑通过。保持源码直接启动，未打包 EXE。

## 2026-09-12 · 根据模型能力显示模式

- 模型选择移到模式 tab 之前，模式按钮由模型目录的 modes 字段决定；未支持的参考模式直接隐藏。
- 所选模型归属整张卡片，切换 tab 不再改变模型。切换模型立即刷新模式与参数区域；旧卡片处于不支持的模式时自动切回首个可用模式，保留各模式草稿。
- 保持现有已接入模型目录；标准版下载完成后的工作流接入不属于此次界面调整。
- Electron 实际检查通过：图片/视频卡片模型选择在模式之前，仅显示两个已支持模式；模型选择事件、独立草稿、自动保存/重新打开、八向缩放、历史点击和全屏预览回归通过。JS 语法与 diff 检查通过。纯前端调整，未重新运行数据库测试或打包 EXE。

## 2026-09-12 · 接入 Z Image 标准版 BF16

- 模型下拉框加入 Z Image 标准版，支持文生图与单图潜空间重绘；使用已校验的 z_image_bf16.safetensors，共用 Qwen 3 4B 和 ae VAE。
- 标准版使用独立负向 CLIP 编码、可调 CFG，默认 40 步 / CFG 4；与 Turbo 切换时刷新默认步数，保留提示词和参考图片草稿。Turbo 仍固定 CFG 1，不显示反向提示词与 CFG 控件。
- 保存和生成 API 接受标准版及反向提示词/CFG；历史展示并可复用这些参数。标准版步数上限 60，CFG 范围 1–20。
- 3 项单元测试、9 项真实 PostgreSQL 集成测试通过；标准版实际文生图与图生图均通过（512×512、30 步、CFG 4），真实进度、负向提示词持久化、私有输出、请求去重与 token 空值均验证。
- Electron 检查通过：模型切换、默认参数、反向提示词草稿、跨 tab 模型保持、保存重开，以及历史预览等原有交互。未打包 EXE。
- 已按追加请求准备 H3-Base-Ref2VA 的 ComfyUI pruned FP8 下载配置（20,958,205,608 字节，固定仓库 revision 与 SHA-256），共用现有文本编码器和音视频 VAE；下载完成后仍需独立接入参考模式工作流。

## 2026-09-12 · LTX-2.5 22B 蒸馏版安装准备

- ComfyUI 已具备原生工作流及所需核心节点；复制文生视频、图生视频和首尾帧模板至本地 user/default/workflows/LTX-2.5。
- 准备官方 Comfy INT8 主模型、Gemma 4 12B 编码器、音视频 VAE 与空间放大器的固定 revision / SHA-256 清单，共 5 文件、39,709,872,236 字节；下载与逐文件校验脚本在 ComfyUI/setup/download-ltx25.ps1。
- 当前 Hugging Face 令牌访问官方 Lightricks/LTX-2.5 权重返回 403 GatedRepo，账号未获准；尚未启动权重下载，需用户先在官方页面开通权限。没有将该模型标记为已安装或加入工作台可用列表。
- 当前 CUDA 12.6 环境禁用了 Comfy Kitchen CUDA 量化加速，INT8 convrot 的运行兼容性及显存表现需在模型下载后检查；尚未调整现有 Torch 环境或做生成测试。

LTX 安装跟进：用户开通访问后，官方权重 HEAD 请求返回 200；已启动五文件断点续传与 SHA-256 校验脚本，并将 LTX 与 H3-Ref2VA 合并纳入每 15 分钟提醒。尚待文件下载完成和实际生成验证。

## 2026-09-12 · 生成进度吸顶

- 生成进度条在卡片内容顶部吸顶，固定 64px 高度；生成信息在进度条下方吸顶，保留 10px 间距，避免两层重叠。
- 无活跃任务时生成信息恢复原来的顶部位置；长状态文字省略显示，进度元素保留完整无障碍说明。
- Electron 使用临时布局数据验证：内容滚动 825px 后，进度仍位于内容顶部内边距处，生成信息与进度条间距 10px；截图检查通过。原有模型切换、草稿保存、历史和全屏预览回归通过。仅修改 CSS，未运行 GPU 或数据库测试，未打包 EXE。

## 2026-09-13 · 修复卡片内滚动的进度固定

- 根据用户反馈，将进度条从 card-content 内部移至标题下方的 card-progress 独立区域；它不再参与卡片内部滚动，也不再依赖 sticky 或 display:contents 的定位关系。
- 每次进度轮询只更新固定区域；生成信息恢复在下方滚动区顶部吸顶。无任务时固定区域隐藏，card-content 设置 min-height:0 保持 flex 内部滚动。
- 上一版只使用临时静态进度元素检查，覆盖不足。本次通过 Electron 实际提交 256×256 / 20 步 Turbo 文生图：内部滚动 672px 后，进度更新至第 4/20 步仍保持同一顶部位置，截图确认位于滚动容器外；生成正常完成。
- 模型切换、草稿保存、历史与全屏预览回归通过，JS 语法和 diff 检查通过。无后端修改，未运行数据库回归或打包 EXE。

## 2026-09-13 · 独立素材卡片与按钮/拖放/粘贴导入

- 画布新增“导入素材”入口，图片、视频、音频可通过按钮选文件、文件拖入、剪贴板文件/图片粘贴导入；创建私有素材卡片并自动保存，支持标题拖动、八向缩放、移除卡片、图片放大和音视频播放。
- 按钮和粘贴导入将素材排列到现有卡片右侧并移动视口；拖入按画布缩放后的落点放置，多文件按网格排列。
- 封面、参考图的原生文件 input 隐藏，以按钮唤起文件选择；封面弹窗和参考图区域支持图片拖入/粘贴，文字粘贴保持原行为。上传期间防止切换项目/退出丢失当前导入。
- 素材接口支持 PNG/JPEG/WebP（20MB）、MP4/WebM 和 MP3/WAV/OGG/M4A/WebM（200MB），按文件特征识别类型；项目保存校验素材归属并从服务端补全文件名/MIME/大小，封面和生成参考仍仅接受图片。
- 私有素材 GET/HEAD 支持 Range 和分块输出，便于视频/音频播放及跳转，避免一次性读出整个媒体文件。生成接口拒绝将音视频作为当前图片参考输入。
- 13 项后端检查通过，覆盖跨用户素材隔离、素材卡片持久化与元信息防伪、Range/后缀范围/非法范围、HEAD、现有生成工作流。Electron 检查通过：按钮导图、视频拖入、音频粘贴、播放元信息、图片放大、参考图拖入、保存重开和所有文件 input 隐藏；截图确认素材与生成卡片共存。
- 追加可选 DIRECTOR_MATERIAL_SMOKE=1 的 UI 检查。修正测试脚本返回函数导致 IPC 无法克隆的问题；隐藏窗口截图会缓存旧画面，最终使用短暂可见的测试窗口完成视觉验证。未打包 EXE。

## 2026-09-13 · 生成等待正计时

- 固定进度条右侧显示“已等待 mm:ss”，超过一小时显示 hh:mm:ss。以服务端任务 submitted_at（旧记录回退创建时间）为基准，包含排队、模型加载和生成等待。
- 使用独立的一秒刷新，仅修改计时文字，不依赖采样进度事件或历史接口返回变化；页面重新显示时立即校正时间，重新打开项目仍从原始提交时间累计。
- 任务结束、返回项目列表或退出工作区时停止无用计时器；计时区域固定宽度行为，长阶段文字省略显示，保持进度栏吸顶布局。
- Electron 真实生成验证：计时从 00:00 增长到 00:02，内部滚动 672px 后仍位于固定区域；既有模型选择、草稿、保存、历史预览回归通过。纯前端修改，未调整数据库或打包 EXE。


## 2026-09-13 · 卡片连线、视频提帧和隐藏生成结果

- 所有卡片增加左侧输入点和右侧输出点，可拖线连接；支持键盘 Enter/空格连接，双击连线或点击来源按钮断开。连线与画布一起自动保存，移除卡片会清理相关连线。连接点外移，避免遮挡左右缩放手柄。
- 下游“引入素材库”展示直接连接的上游导入素材及已完成的全部生成输出，新生成结果随历史刷新出现。图片可用作图生图/视频首尾帧输入；生成图片先保存为私有素材快照。音视频可预览，作为模型输入仍受当前模型能力限制；连接不会自动执行生成。
- 导入视频和当前生成视频增加“提取最后一帧”“提取指定帧（秒）”，使用浏览器解码原始分辨率画面并保存 PNG 图片素材卡片，可继续连接和用于生成。检查空秒数、负数、超出时长、无法解码及读取超时。仅支持浏览器可解码的视频。
- 生成结果可勾选隐藏；普通历史和固定对比区排除隐藏结果，独立“已隐藏”列表支持预览、恢复。隐藏状态随用户项目自动保存，不删除原始文件，不撤销已有下游输入快照。
- 工作台长文本采用 word-break: break-all 和 overflow-wrap: anywhere，卡片标题、进度文字与素材名称允许换行。
- 后端校验连接 UUID、当前画布端点、自连接、重复连接以及隐藏结果列表格式。

图生图提示词参考：当前 Z-Image 使用输入图编码后重绘的工作流。提示词描述最终希望得到的完整画面，例如“白色陶瓷杯放在深色木桌上，夜晚咖啡馆，窗外霓虹灯虚化，蓝色环境光与暖色桌灯交织，电影摄影，浅景深”。可从重绘强度 0.35 试起；较低值通常更保留输入，较高值改动更大。此数值是使用建议，并非官方固定推荐；不保证精确保留人物身份或局部细节。机制参考：[ComfyUI 图生图文档](https://docs.comfy.org/tutorials/basic/image-to-image)。

验证结果：

- 原有后端/进度测试 14 项通过；新增隐藏字段校验后，连接与隐藏状态集成测试单独复验通过（真实 PostgreSQL，244.57 秒，含启动与关闭）。
- Electron 实际鼠标操作验证八方向缩放、连接点拖线、生成结果引入、输入图保存；下游实际 ComfyUI 图生图成功，并重新打开确认结果图片加载。
- 勾选隐藏、独立隐藏列表、保存后重新打开及恢复通过；确认长文本计算样式为 break-all。
- 使用真实 MP4 提取末帧和 0.5 秒帧，生成有效 PNG 素材卡片，重新打开仍保留；原有图片按钮导入、视频拖入、音频粘贴及播放元数据检查通过。
- 查看真实 Electron 截图后调整复选框横向对齐、视频预览最小高度和新卡片间距。修正 UI 测试对已隐藏旧画布的误判：重新打开须等待编辑器可见并按项目 UUID 选择，生成图片须等待解码完成再检查尺寸。
- JavaScript 语法检查、git diff --check 通过。验证使用独立测试账号和数据，未改动真实账号数据。


## 2026-09-13 · 提帧工具行样式调整

- 导入视频及当前生成视频的提帧操作移到预览画面外的独立行，避免挤占视频画面和播放控件。
- 统一为深色描边按钮，左侧“最后一帧”，右侧秒数输入和“按秒提取”；秒数单位内嵌，清理原生数字步进按钮并保留键盘输入及焦点提示。
- 保持原有提帧事件与参数不变。检查 JavaScript 语法和 diff；用真实 Electron 视频素材预览确认 380px 卡片下工具行在视频下方、按钮横向对齐且不溢出，并查看截图。


## 2026-09-13 · H3 Ref2VA 与 LTX-2.5 工作台接入

- 新增 H3-Base-Ref2VA FP8 视频模型：仅显示多图参考 tab，支持 1–8 张图片，按 Picture 1、Picture 2 编号；提示词可使用 `<Picture 1>` 引用。默认 512×320、5 秒、20 步。使用独立 ref2va 权重及原生 ReferenceToVideo 节点，不混用 fl2va Turbo LoRA。当前入口仅支持图片参考，视频/音频作为参考尚未接入。
- 新增 LTX-2.5 22B 蒸馏版 INT8：文生视频与单首帧图生视频，采用已验证的原生两阶段 8＋3 步工作流；首帧同时进入两阶段。默认最终输出 512×320，宽高为 64 的倍数，步数固定 11。原生音频和视频解码、潜空间放大器一并接入。
- 模型控制模式 tab、参考图数量、默认尺寸/步数/时长与尺寸步进；上游连线生成图片可进入 H3 Ref2VA 参考库。LTX 进度按两阶段显示，保留卡片固定进度栏及正计时。
- 后端允许新模型的项目草稿保存，并校验模式、尺寸、固定步数、参考图片 UUID、数量和访问权限；输出仍走已认证的私有任务接口。更新 README 和可选 GPU smoke 脚本。
- 验证：14 项后端测试通过；Electron 检查 Ref2VA 模式、LTX tab/只读步数/尺寸步进/首帧标签以及保存后重新打开通过；JavaScript 语法与 diff 检查通过。
- LTX 首帧图生视频经工作台 API 实测通过：256×256、约1秒、11步，耗时129.233秒（含重新加载），两阶段真实采样进度、幂等提交、历史保存、私有输出及 token 未提供的正确展示均通过。此前原生文生视频全流程也已通过。

- H3 Ref2VA 双图参考经工作台 API 实测通过：256×256、20步、输入时长1秒按网格输出39帧，耗时290.886秒（含首次加载）；真实进度、幂等提交、参考图数量/模式拒绝、历史与私有输出均通过。
- 两个实际输出均检查 H.264 视频与 AAC 音轨可解码，并查看中间帧；LTX 25帧，H3 39帧。测试尺寸仅验证集成链路，不能代表高分辨率、长视频和大量参考图的显存或质量表现。
- 已正常重启用户的源码 Electron 工作台加载新模型列表，未打包 EXE，未更换驱动、PyTorch 或已有模型文件。


## 2026-09-13 · H3 视频与音频参考接入

- Ref2VA 支持图片、视频、音频混合参考，共最多 8 项，视频/音频各最多 3 项。素材按钮、拖入、粘贴、上游连线库支持这些类型，上游生成视频也可复制为私有参考素材。
- 输入区域按类型显示 Picture / Video / Audio 编号和播放器；项目、历史保存服务端确认的 MIME/名称，重新打开与复用参数可恢复。其他模型仍限制为图片，封面图片限制和素材归属检查保留。
- 使用本地 PyAV 18.1.0 准备参考：视频开头按生成时长截取、24fps、限制像素面积；音频转换 48kHz 双声道。通过 ComfyUI 原生 LoadVideo/GetVideoComponents/LoadAudio 接入 Ref2VA。视频只作为画面参考，声音单独添加音频；界面已明确说明。临时转换文件自动清理，原素材不变。
- 实测两张图片＋视频＋音频同时输入，任务 `969bc3ac86c842bf914ea711f5d9d9df` 完成，输出 H.264＋AAC 可解码；真实进度、私有输出、幂等提交、历史与非法数量拒绝通过。测试为 256×256、20 步、1 秒输入时长，不能据此保证大量参考、高分辨率任务的显存/生成质量。
- 原有 14 项后端测试通过；新增 2 项实际音视频转码测试通过，验证 30→24fps、尺寸/时长限制及音频重采样。Electron UI 测试通过视频/音频连线引用、图片上传、编号与预览、自动保存/重开和模型能力过滤。
- 保持源码 Electron 启动方式，不打包 EXE。


## 2026-09-13 · 生成队列取消与停止

- 卡片顶部固定进度区域增加“取消排队 / 停止生成”。优先展示运行任务，其他任务列在可展开队列中，可逐个取消；展开状态在进度刷新时保留。提交尚未返回任务 ID 时按钮暂不可用。
- 新增用户归属校验的任务取消接口，调用 ComfyUI 原生 `/api/jobs/{prompt_id}/cancel`，按任务 ID 原子取消，不使用全局 interrupt 或清空队列。
- 接受请求后显示“正在停止”，通过队列/历史确认结束后记为“已停止”；重复请求幂等、已完成结果保留。历史写入增加乐观并发检查，避免旧轮询覆盖取消状态。无法确认停止时提示错误，不伪报成功。
- 16 项原有后端测试与 12 项取消专项测试通过，覆盖归属拒绝、按 ID 调用、重复停止、完成竞争、取消确认及并发保护。Electron 模拟队列 UI 测试通过固定按钮、分任务取消、展开状态和历史显示；截图 `.test-data/studio-smoke/cancel-ui.png`。
- 真实 GPU 取消测试已准备为 `scripts/smoke-cancel.py`，因检测到用户正在运行的任务自动退出，本次未执行真实中断测试。组合测试命令曾被自动审批拦截，拆分为可审查脚本后获准运行；没有改动用户的 ComfyUI 队列。


## 2026-09-13 · 画布汇总队列、尺寸提示与视频 PIN 同播

- 画布右侧增加可收起的当前项目汇总队列，展示模型、提示词、尺寸和实际排队位置。支持定位卡片、逐项停止/取消、拖拽及上下按钮排序；运行中的任务不参与排序，窄屏侧栏覆盖显示。
- 本地 ComfyUI `director_queue` 扩展在队列 mutex 内重排选中任务占用的槽位，保留任务 ID、工作流、其他项目/用户的队列位置及运行任务。后端校验项目归属、任务归属和排序快照；队列变化返回冲突，避免删除后重新提交导致重复生成。
- 扩展安装到本机 custom_nodes；先同步生成记录并备份 ComfyUI 历史，确认队列为空后重启加载。`scripts/install-queue-extension.py` 供其他 clone 用户显式安装，不修改 ComfyUI 核心文件。
- 实际 GPU 队列测试通过待执行任务换序、ID 不变、过期顺序拒绝、运行任务拒绝；同时补足上次因用户生成而跳过的真实取消验证：排队移除、运行中断、幂等停止、后继任务完成、完成文件保留均通过。
- 模型接口统一返回本机尺寸范围、步进、总像素预算和推荐值。前端提供一键推荐尺寸、即时错误及提交拦截；后端拒绝越界、步进不符、总像素过大及小数尺寸。明确本机限制不代表模型理论上限。
- 修正 Tornado 将非 ASCII HTTP reason 变为 Unknown 的问题：中文校验原因进入 JSON，HTTP 状态使用标准 reason。真实接口验证能返回具体宽度/步进/像素错误。
- 视频 PIN 增加“同时播放”，至少固定两段视频后可启用；联动播放、暂停、进度和倍速，取消后独立控制，偏好自动保存。保留已有 PIN 媒体 DOM，切换历史不打断播放；短视频自然结束不会停掉其他视频。
- Electron 验证通过队列拖拽/箭头/定位/取消/收起、尺寸提示/推荐/非法提交拦截，以及两段真实私有视频的同播/暂停/跳转/倍速/取消联动/历史切换/重新打开设置。测试产物保留于忽略目录 `.test-data/studio-smoke`。

- 本次完整后端测试：44 passed；JS 语法、差异空白检查通过。


## 2026-09-13 · 放大预览右键复制图片

- 图片预览区域右键弹出小型“复制图片”菜单，菜单位置限制在窗口内；同时提供工具栏按钮及 Ctrl+C，选中文字时不劫持普通复制。Esc 优先收起菜单，点击外部也会收起。
- 使用已加载图片的原始像素绘制透明 PNG，再通过 ClipboardItem 写入系统剪贴板。缩放和平移不改变复制内容，异步复制绑定点击时的图片，切换图片后不会显示过期成功提示。加载失败、权限拒绝及编码失败均有反馈。
- 增加 Electron 剪贴板冒烟脚本 scripts/smoke-preview-copy.cjs，测试私有上传图片、右键位置、实际系统剪贴板像素和尺寸、Esc 及 Ctrl+C。

- Electron 实测通过：缩放后右键复制，系统剪贴板 PNG 的原始尺寸及完整像素与源图一致；窗口边缘菜单、Esc 收起及 Ctrl+C 通过。JS 语法与差异检查通过。


## 2026-09-13 · 耗时自动换算单位

- 实时等待计时和历史耗时统一按秒、分钟秒、小时分钟秒、天小时分钟秒显示，整分钟/整小时/整天正确进位。按整秒展示，历史悬停保留原始毫秒数，数据库数值不变。
- 验证 59/60 秒、3599/3600 秒、86399/86400 秒及跨天组合；缺失耗时显示“待返回”，负等待时间归零。JS 语法检查通过。

## 2026-09-13 · 耗时补充总秒数

- 实时等待与历史耗时在分钟及以上的换算结果后，使用括号展示总秒数，例如“1分钟 30秒（90秒）”；不足一分钟直接显示秒数，避免重复。
- 验证分钟、小时、天的边界与总秒数一致，JS 语法和差异检查通过。

## 2026-09-13 · 生成历史参考素材缩略图

- 生成信息新增“本次使用素材”，读取生成记录保存的 refs/ref_info，按当次顺序展示图片、视频、音频和名称；标注输入图片、首尾帧或 Picture/Video/Audio 编号，不受当前卡片参考素材修改影响。
- 图片点击进入全屏预览并在本次参考图片之间切换，支持现有复制功能；视频和音频使用播放器。无参考素材明确提示，旧记录缺失名称时使用类型标签。长文件名断行，素材横向滚动。
- 验证混合素材类型、历史顺序、文件名转义、旧记录首尾帧标签、空素材与不同历史独立渲染；两个 JS 文件语法及差异检查通过。

## 2026-09-13 · 模型切换到 I 盘固态

- 完成 16 个模型权重及 11 个配置的复制，共约 138.08 GB；逐文件 SHA256 校验通过，J 盘原件保留。通过 ComfyUI 原生 extra_model_paths.yaml 设置 I 盘优先，无需重装。
- 用户生成完成后才重启，检查工作台未完成记录并备份 4 条 ComfyUI 历史；原启动参数保留。启动日志、16 个模型路径优先级、节点模型选项、队列扩展及工作台 HTTP 验证通过。本次未重跑全部模型生成或测量性能。
- 本机迁移路径、校验清单、验证范围及回退方法见 docs/SSD-MIGRATION.md；数据库、上传素材、输出目录均未移动。

## 2026-09-13 · 清理 J 盘旧模型与下载分块

- 用户明确要求不再保留旧模型，删除已迁移的 16 个 J 盘权重及对应 16 个分块目录，释放约 275.58 GB。原有小型配置和占位目录保留。
- 核对固态文件大小、已完成 SHA256 清单及原生模型路径；按明确路径清理普通文件和空目录，删除后再次验证 I 盘 16 个模型优先解析及服务模型列表。
- 更新迁移说明：J 盘不再有模型备份，回退前须先复制模型。

## 2026-09-13 · 项目列表 ComfyUI 连接配置

- 列表页新增黑白风格配置弹窗，支持 Host/Port、地址预览、默认值、测试连接和保存反馈。本机项目共用，隐藏 .comfyui.json 持久化，保存前验证服务有效性，失败不覆盖旧配置。
- 生成提交、素材上传、模型在线状态、队列排序及 WebSocket 进度使用配置地址。生成记录保存当次 comfy_url，历史结果从原服务读取；旧记录默认 127.0.0.1:8188。切换地址与生成提交共享锁，未结束任务阻止切换，切换时关闭旧进度连接。
- 新增连接校验、持久化、失败保留、忙时保护、测试不保存、HTTP/WebSocket 地址和历史输出归属测试。Electron 实测默认配置、真实 ComfyUI 连通、保存、错误端口、恢复默认值和重新打开通过，截图保留在忽略的测试目录。
- 完整后端回归 61 passed；JS 语法及差异检查通过。已重启源码 Electron，实际工作台 HTTP 与新配置入口验证正常。

## 2026-09-13 · 部署与架构 Skill 文档

- 仓库根目录增加 SKILL.md，包含 YAML 技能说明、Windows 源码部署、ComfyUI 连接、组件关系图、三库分工、生成流程、数据与备份边界及排查入口，供新用户和代码助手阅读。命令和配置路径依据现有源码核对，不包含个人账号、素材或数据库。
- README 增加入口并修正过时的“导演编辑功能尚未实现”说明。Skill 验证器及文内本地链接检查通过；本次仅文档变更，没有重跑安装或修改用户数据。


## 2026-09-13 · 多厂商云存储直传

- 新增七牛 Kodo、阿里云 OSS、腾讯云 COS 独立配置，厂商对应密钥字段、地域提示、自动 Endpoint 与可选 CDN 域名。
- 凭据写入隐藏配置文件（0600），设置响应不回显；长期密钥仅后端使用。后端签发单文件短时凭证，客户端直接 POST / PUT 到云存储，确认对象与公网访问后填入模型参考字段。
- 参考字段支持选择文件、拖入、粘贴及失败重试；上游素材可由客户端读取后直传。保留原有本地导入及 ComfyUI 流程。
- 增加实际 SDK 签名、配置隔离、权限、确认失败与错误脱敏单元测试；扩展隔离 Electron 冒烟，模拟三家外部上传并验证 URL 回填、配置切换和生成回归。未提供真实存储凭据，未执行真实云上传。

- 云存储新增每厂商独立 `path_prefix` 与地址预览；前缀参与实际对象 Key 和访问 URL，空值为根目录，校验路径字符并兼容旧配置。

- 新增画布素材直传模式：选择文件、拖入和粘贴可创建云素材卡片，服务端验证上传归属和完成状态，公网预览及云模型 URL 复用。
- 客户端分块计算 MD5（SparkMD5 3.0.2，随源码附许可证），对象以 MD5 命名；项目内按配置和内容查询完成记录，复用重复素材，待确认记录使用相同对象 Key，避免产生额外对象。

- 对象目录增加项目 block_id。修复设置/确认错误路径中的未定义 grant 引用，补充 SOCKS 依赖；公网确认支持安全检查后的 HTTP→HTTPS 跳转。对实际已上传七牛对象完成只读元信息和公网 HEAD 验证。

- 上传新增常驻阶段进度面板，MD5 分块计算百分比、XMLHttpRequest 实际上传字节进度、多文件序号、公网确认、去重复用及错误保留反馈。

- Seedream 按模型扩展多图融合、Pro 交互编辑、Lite/4.5 组图模式及张数校验；输出格式、水印、Pro 提示词优化选项；保存全部组图结果并显示逐张预览，增加模式与参数回归验证。

- 修复云图片复制时 tainted canvas：预览关联自有云素材读取端点，复制时获取原图并解码为干净位图；限制已确认的图片、验证归属、公网地址、大小和 MD5。用无 CORS 的独立图片源复现污染，并验证复制 PNG 像素与原图一致。

## 2026-09-14 · Seedance 2.5 时长修正

- 移除视频统一 15 秒上限，按模型元数据校验；豆包与 Dreamina 2.5 前后端均支持 4–30 秒，旧模型仍为 4–15 秒。覆盖三种输入模式、越界值和 UI 切换回归，未发起付费生成。

- service-inference 新增多 Key 命名管理、单选启用与逐 Key 模型发现；按图片/视频展示已接入和未接入模型。旧配置兼容迁移，凭据不回显，后台任务绑定原 Key。切换后卡片保留原模型/参数，不可用模型禁止生成；增加迁移、切换、忙碌保护、模型过滤和界面回归验证。
## 2026-09-13 · 开放本机 ComfyUI 局域网访问

- 按用户要求在本机 ComfyUI 启动脚本及当前服务加入 --listen 0.0.0.0 --port 8188，保留 GPU 与显存参数；确认队列为空并备份历史后重启。
- 新增 Windows 入站规则 HotpoorDirector-ComfyUI-LAN-8188，限定 ComfyUI Python、有线网卡、Private 网络、LocalSubnet、TCP 8188。未配置路由器公网映射，工作台与数据库监听不变。
- README 和 SKILL.md 补充局域网启动方法与回环队列排序扩展的使用边界。
- 验证当前进程监听 0.0.0.0:8188；本机分别经 127.0.0.1 和 192.168.100.100 访问 system_stats 均正常，防火墙范围已核对。尚未从另一台局域网设备实测。

## 2026-09-14 · 评论区卡片与链式消息分包

- 新增评论区卡片与独立 chat entity；项目库以 chat_pack 实体按 25–100 条分包，双向链指针、头尾计数、事务行锁和消息 ID 幂等。正文逐条落库，修改容量只影响新包；画布只保存 chat_id。
- 卡片支持重命名、分包设置、刷新与向前加载，纯文本 / Markdown / HTML / contenteditable 富文本，发送中状态与失败保留草稿。富文本经 DOMPurify 白名单渲染，静态依赖随源码包含许可。
- 评论图片/视频支持本机文件上传、云存储直传、拖入/粘贴、HTTP 地址和项目卡片资产/生成结果引用；引用不复制文件。上传显示进度，线程和素材请求沿用账号权限、XSRF 与大小限制。
- 增加隔离数据库的并发分包/幂等/权限回归和 Electron 评论卡片集成验证；不触发付费生成。

- 验证结果：152 项后端测试通过；评论区 Electron 冒烟验证四种文本、真实可解码视频附件、地址/引用、分包翻页及重开；原云端生成 UI 回归通过。

## 2026-09-14 · 云存储同厂商多配置

- 云存储设置新增同时可见的列表和编辑表单，配置名称、添加/编辑/删除、单选启用、仅保存与停用；同一厂商可保存多组 Bucket / 域名 / 前缀 / 密钥。
- 兼容旧厂商配置 ID 与上传记录；上传绑定配置 ID，确认不随当前启用项改变；改名不影响历史去重指纹。密钥留空仅复用正在编辑的那套配置，新配置不会继承其他密钥。
- 增加同厂商多配置保留、切换、失败不覆盖、仅保存、密钥隔离和旧上传确认测试；扩展隔离 Electron 测试，验证两套七牛配置、实际选中路径和切回原配置。

- 验证结果：154 项后端测试通过；扩展后的云存储多配置与原生成界面回归通过；本机原有配置及启用状态读取正常。

## 2026-09-14 · 评论卡片关联与非破坏性媒体标注

- 评论卡片新增左右连接点，接收上游素材并将评论媒体和说明提供给下游；沿评论包索引加载历史附件、缓存已满包，保留原素材 ID / URL 与画布连线。
- 评论附件增加图片框选/涂鸦编辑器和视频起止时间引用；SVG 由受限相对坐标生成，支持撤销、清空、颜色，视频选段播放到结束暂停。历史附件可以再次引用并标注。
- 服务端校验媒体类型、坐标范围、形状/点数、颜色、时间范围和账号归属；附件标注参与消息幂等摘要，重开后恢复。只保存标注数据，不新增媒体副本或执行付费生成。
- 新增标注白名单和非法输入回归、媒体索引权限与存储回归；扩展 Electron 实测两向连线、鼠标框选和涂鸦、视频片段停止、无重复上传及重开恢复。

- 验证结果：169 项后端测试通过；评论关联、图片 SVG 标注、视频区间播放、历史附件索引及重开恢复的 Electron 回归通过，原云存储与生成界面回归通过；已重启本机工作台加载更新。

## 2026-09-14 · 评论显示紧凑化

- 收紧已发送评论的文字、元信息、按钮和间距；媒体改为紧凑缩略图，最高 160px，长文件名最多两行。修复带 SVG 标注图片绕过高度限制导致评论被撑大的问题，草稿标注图限制为 70px；原图比例与 SVG 覆盖对齐，保留完整查看和播放入口。
- 验证：评论 Electron 界面回归通过，覆盖发送、连线、SVG 标注、视频区间播放及重开；已检查渲染截图。开发工作台自动加载样式更新。

## 2026-09-14 · 评论附件临时调整预览尺寸

- 评论图片、视频及草稿附件加入独立右下角拖拽手柄，按画布缩放换算拖动距离，可调整显示宽高；双击恢复紧凑预览。只改变当前 DOM 显示，刷新或重新渲染后恢复，不写入评论和媒体文件。
- 放大附件单独占行，宽度限制在评论区内，图片和视频保持比例，SVG 以实际图片边界同步缩放；原有查看标注和视频区间播放保留。
- 验证：Electron 实际鼠标拖拽图片和视频均可增大宽高，双击复位、SVG 对齐及原有评论完整回归通过。

## 2026-09-15：恢复 UUID 求余实体分库

- 主库 `hotpoor_director` 保持索引及认证职责；新增 `index_entity_commits` 记录跨实体库事务提交决定。
- 所有实体按 `int(block_id, 16) % 2` 路由（0 → director1，1 → director2），项目、素材、评论、消息包、任务和云上传不再按业务类型选库。两库仍各只有 `entities` 一张表。
- 统一 EntityStore 处理显式 ID 路由和双库列表汇总排序；跨库写入通过两阶段提交及恢复日志处理，实体事务在索引库上加 advisory lock 串行执行。
- 初始化在实体表 DDL 前恢复未完成事务；内置 PostgreSQL 启用 `max_prepared_transactions=32`。
- 旧数据迁移先生成本地 JSONL 备份，再复制、校验、删除错误分片副本，保持 UUID、内容和时间戳；遇到同 ID 内容冲突停止，避免覆盖。物理表约束阻止后续错库写入。
- 使用独立 PostgreSQL 测试实例验证路由、列表、评论、跨库回滚、提交中断恢复、旧数据迁移与重复初始化；全量回归 177 项通过，补充重复 UUID 迁移用例后，集成测试 23 项通过。
- 用户关闭 Electron 后，已完成本机真实数据库迁移：31 条实体中移动了 13 条，最终 director1 为 15 条、director2 为 16 条，错库记录与重复 UUID 均为 0。迁移前三库完整备份已保存于 `.local/backups/before-uuid-sharding-20260915-082705/`；全部实体 UUID、JSONB 与时间戳的整体 SHA-256 一致，再次初始化结果不变。验证结束后已停止本次启动的 PostgreSQL，可重新打开 Electron。

## 2026-09-15：补齐云端生成与存储 Skill 说明

- `SKILL.md` 增补 service-inference 多 Key、模型发现、任务凭据绑定、重启查询及结果保存说明。
- 增补云存储三厂商多配置、客户端直传、上传确认、MD5 复用及上传记录按自身 ID 分片的规则。
- 同步架构图、代码入口、隐藏配置与生成文件位置、云端排查路径；部署检查按实际使用的本地或云端服务执行。
- 本次仅更新说明文档；Skill 结构校验与 Git 空白检查通过，未调用云服务或修改用户配置。

## 2026-09-15：README 以八个特色组织截图说明

- 首页按用户确认的八个特色介绍：多图 / 多视频 PIN、评论片段播放、参考连线、模型 Tab 指引、本地与云端模型混用、多配置云存储、无限画布、复用原图的独立图片标注；同步 SKILL 的产品介绍重点。
- 截图放入 `docs/screenshots/` 并内嵌到对应章节，注明测试素材与模拟云服务范围；补充来源、采集版本和复现步骤。
- 新增 `scripts/capture-readme-scenes.cjs`，由 `smoke-inference.cjs --readme` 生成多图 / 多视频 PIN 和模型 Tab 对比画面；`smoke-comments.cjs --readme` 增补视频片段与参考线全景。只操作独立测试数据。
- 界面验证覆盖真实 PIN 播放 / 暂停 / 进度 / 倍速联动、按模型呈现不同 Tab、评论片段边界、引用连线、标注不重复上传及重开恢复；不调用实际云存储或付费生成接口。
- 最终验证：两个带 `--readme` 的 Electron 界面脚本均通过；7 张 PNG 完整性、README 图片路径与章节锚点、截图清单 SHA-256、Skill 结构及 Git 空白检查均通过。

## 2026-09-15：云端工作站与双向版本同步

- 新增「同步云端」：多个 HTTPS 域名 / 产品路径、独立 AK、系统浏览器登录授权、按项目和目标启用自动同步、卡片目标状态与新版本提示。
- 三方 diff 使用共同版本 / 本地 / 云端内容哈希，检查预览后的修改；保留旧项目、修改前后历史及云端提交，不按设备时间静默覆盖。历史分页可继续读取更早事件。
- 从线上拉回时创建新项目、卡片、素材、任务、评论块、消息与连线 UUID，并修正 PIN、封面、参考图和富文本素材关联。原本地项目保留；回传新 UUID 映射与接收记录，重试不会无限复制。
- 首次向目标同步本地媒体时使用云端签发的短期存储凭证，增加该目标的 `cloud_versions`；原文件保留。导入的云端素材用于本地 ComfyUI 时按需下载并验证大小 / SHA-256。暂不提供自动逐字段合并或一键删除原文件。
- 共用后端及前端通过导出脚本与 SHA-256 清单同步到 API 仓库。云端沿用 API 账号，独立 `hotpoor-director-postgres` 监听 5544，UUID 求余分为两库，开启跨分片事务；原有 API PostgreSQL 没有重启。
- 云端 AK 有效期 90 天，支持撤销，仅允许读取、同步和存储上传；修改云端设置 / 执行模型必须浏览器登录。按云端账号隔离配置与推理管理器。
- 已部署 `api.xialiwei.com/hotpoor/director`，正式端口 8613、镜像 `xialiwei-api:0.10.0-director`；保留原服务与 Nginx 回滚备份。
- 验证：全量 195 项后端测试通过；同步、云端生成 / 存储、评论 / 标注 / 片段三个 Electron 脚本通过；候选和正式 HTTPS 各 22 项 Director 检查通过，原 API 72 项 ACL 与 35 项多画笔配置检查通过。验证账号与测试实体已清理，无付费模型请求或邮件发送。
- README 新增实际同步差异截图，更新来源 / 校验清单、`docs/CLOUD-SYNC.md` 与 SKILL；同步测试使用独立数据，云存储上传链路中的外部传输在新增双端测试中以适配器模拟。

## 2026-09-15：访问密钥有效期由用户选择

- 取消新密钥固定 90 天；客户端授权和手动创建都要求明确选择预设时间段、自定义日历日期或永不过期。
- 多密钥列表显示用途、创建 / 到期 / 最近使用时间和状态；支持调整已有密钥有效期及单独撤销，保留原有期限直到用户修改。
- 数据库以 NULL 表示永不过期，迁移不改写旧密钥。验证涵盖永久 / 到期密钥、无效输入、跨账号修改隔离及撤销后无法恢复。

- 浏览器 favicon 改用现有黑色原版 Logo PNG，并使用独立文件名避免旧图标缓存；macOS 启动时显式设置 app.dock 图标，修复源码 Electron 显示默认原子图标。
- 本次验证：5 项云端网关集成测试、密钥表单 Electron 检查、真实 Electron 源码启动 / Dock 图标检查、候选服务与正式 HTTPS 各 28 项 HTTP 检查通过；使用隔离数据与临时账号。

## 2026-09-15：统一云端导航入口样式

- 客户端授权 / AK 入口复用退出登录的 quiet 按钮样式，修复浏览器默认紫色下划线链接；跳转前保存当前项目。

## 2026-09-15：同步云存储提示

- 自动同步错误使用简短「需处理」导航状态，完整错误按项目和云端显示在同步窗口；成功重试后清除。
- 未启用存储时明确指向目标云端的「保存并启用」，说明两端前缀可以不同，避免误导用户修改本地配置。

## 2026-09-15：队列切换同步进度

- 队列增加生成 / 同步 Tab，复用现有侧栏样式，支持键盘方向键切换。
- 按当前项目和云端显示比较、资源上传与提交、拉回与回传、待处理差异、失败原因和最近成功时间；没有字节进度时不估算百分比。可直接打开对应云端处理差异或重试。

## 2026-09-15：同步读取历史 ComfyUI 资源失败

- 捕获原 ComfyUI 网络不可达与超时，返回明确的 502 提示；同步错误附任务 UUID 和结果序号，保留已上传资源，不跳过缺失内容。
- 配置读取成功不再掩盖同步未解决错误。13 项同步测试通过，包含资源读取失败回归。

## 2026-09-15：服务器仅云存储

- 服务器导入与评论附件固定云存储，后端拒绝本机上传；封面、参考图和截帧通过云存储上传后创建无本地文件的资产引用。
- 云端生成前检查存储配置；生成结果在有大小限制的内存中转存，数据库保存 HTTPS URL，不创建服务器 generated 文件。现有历史文件保留。
- 云端网关与同步 20 项测试通过，包含拒绝落盘、远程资产重定向及生成成功 / 失败均无本地媒体文件；评论 Electron 回归通过。
- 最终云端网关 / 存储 39 项测试通过；候选和正式 HTTPS 各 30 项检查通过，服务器已切换到 8616，保留旧 worker 与回滚配置。

## 2026-09-15：在线项目分享与成员独立 AK

- 项目增加「分享与成员」：按已验证邮箱邀请、四档权限、独立生成许可；只读链接支持可选有效期 / 永不过期，登录加入及撤销。
- 网关按项目范围校验资源、成员角色、链接到期和撤销；成员不能注入其他项目私有资源，配置接口始终隔离到当前账号。
- 生成绑定实际发起人 credential_owner，凭据与云存储均读取成员自身配置，缺失时明确拒绝，不使用所有者 AK；重启后恢复原成员任务管理器。
- 修改历史记录实际操作人；保持 revision 并发检查及 UUID 新副本权限隔离。公开 CDN 地址不随工作台分享撤销自动失效。
- 全量 201 项测试通过；补充链接到期、跨项目资源注入、成员配置隔离回归，9 项云端网关测试通过；Electron 分享表单、成员权限、只读界面检查通过。未发送邀请邮件或调用付费模型。
- 最终候选 / 正式 HTTPS 各 43 项检查通过，部署到 8618（0.11.1-director-collaboration），旧 worker 与回滚配置保留。密钥忙碌检查也按 credential_owner 隔离，避免跨账号同名配置相互影响。

## 2026-09-15 · 统一最上层遮罩提示

- 新增共用异步提示 / 确认 / 输入组件，使用浏览器 top layer，深色遮罩与按钮统一；支持 Esc、焦点恢复、连续请求排队，文本按纯文本显示。
- 卡片移除改为自定义确认，取消不删除，确认后重新核对项目、卡片和评论忙碌状态；避免异步等待时误操作另一个项目。
- Electron 启动失败和服务退出使用同样的提示样式，离线页面无需后端，用户确认后退出；删除原生系统错误弹窗调用。
- 验证：两个 Electron smoke 通过，覆盖遮罩层级、键盘、360px 布局、取消 / 删除持久化、项目切换保护及无后端启动错误；同步与云网关 22 项测试通过。README 新增隔离测试截图。
- 线上已部署 `0.11.2-director-dialogs`（8619），候选与生产 HTTPS 各 45 项检查通过；保留旧 worker。

## 2026-09-15：云端素材准备进度与生成结果转存

- 桌面提交按工具栏保存方式选择本机/云存储；在线强制云存储。保存配置随任务固定，不因生成期间切换配置而写入其他空间。
- 统一两端结果转存，复用厂商签名、项目/MD5对象路径、上传记录与对象/公网验证；验证前不标记生成完成。云存储路径不创建 generated 文件。
- 准备阶段加快只读轮询，过滤并透传上游进度计数；未返回细分进度时明确显示最近查询。结果读取/上传持续返回实际字节数，界面区分读取、上传和校验。
- 视频完成结果地址持久化，保存失败及重启继续原任务；错误显示具体阶段、重试次数与可公开原因，不回显密钥或原始服务响应。
- 初步验证207项后端测试、Electron云模型/素材/历史/窄屏界面检查通过；相关上游响应与上传服务在测试中模拟，不提交付费生成。在线实际异常另行核验。

- 线上核验：原任务上游状态为 completed，结果位于海外 TOS，服务器读取出现超时；升级后同一任务恢复为 reading_output，已读到实际字节，保存目标 cloud，无再次生成提交。
- 部署：候选目录权限问题修正后45项HTTP检查通过；首次Nginx切换即时校验命中旧worker并自动回退，增加重载等待后成功切换8620。镜像 `xialiwei-api:0.11.3-director-progress`，正式HTTPS45项检查通过，旧worker停止并保留回滚。
- 桌面后端需重启客户端加载；在线用户刷新页面加载新进度界面。

- 进一步定位实际结果为23,796,812字节（约22.7 MiB）；跨境读取吞吐低于原5分钟下载时限，导致循环从头读取。新增七牛远程抓取（稳定任务对象名、完成对象复用、验证后登记），其他转传上限30分钟。上游结果作为参考时直接复用已保存云地址。新增抓取重试/验证测试，相关107项与Electron界面检查通过。

- 最终线上版本 `xialiwei-api:0.11.4-director-transfer`（8621），候选/正式HTTPS分别45项检查通过，旧容器停止保留。全量208项后端测试、Electron云端工作流通过，49份导出文件与API仓库清单一致。
- 原异常任务 `7dbfef0322fb4d05ab66944e6358d884` 已恢复为 completed，云存储方式 remote_fetch，23,796,812字节；公开Range读取返回206且MP4 ftyp文件头有效，未再次提交生成。七牛已完成抓取但任务仍在备用读取时，确认无未受理生成提交后重启工作进程，复用已有对象完成校验与任务更新。

## 2026-09-17 · 网页端管理 AK 与费用界面同步

修复网页端仍使用旧 Director runtime 的问题，通过 export-cloud-runtime.py 同步 API 仓库共用实现。基于当前生产镜像构建 0.11.5-director-management，候选与生产 HTTPS 各通过 49 项检查，包含新管理脚本、生成 AK 绑定入口、独立配置读取及匿名拒绝。生产切换至 8622，保留 8621 worker；Nginx 备份 /home/ubuntu/director-management-20260917/nginx-before-management.conf。未调用付费生成或复制用户凭据。

## 2026-09-17 · 费用默认全部历史

管理费用查询新增并默认选择「全部历史」，API 默认通过显式 UTC 日期范围 1970-01-01 至今天查询，避免上游默认 30 天或周期最长 90 天造成截断。用户可继续指定日期或选择快捷周期。8 项管理测试通过（包含默认全历史三接口参数），候选及正式 HTTPS 各 49 项协议检查通过。生产镜像 0.11.6-director-all-history，端口 8623；保留 8622 worker 与 /home/ubuntu/director-management-all-20260917/nginx-before-all-history.conf。

## 2026-09-17 · 每张生成卡片选择 AK

图片/视频卡片新增生成 AK 下拉，支持不同卡片混用不同 AK，不再来回切换设置页。模型接口提供当前账号的多 AK 模型可用范围，卡片按所选 AK 筛选并持久保存 credential_id；提交只使用所选 AK，缺失配置不回退，历史记录保存实际 AK 名称和绑定。233 项测试及 Electron 模拟生成验证通过，包含不同卡片分别使用不同 AK 与持久保存；候选和生产 HTTPS 各 49 项协议检查通过。生产镜像 0.11.7-director-card-ak，端口 8624，保留 8623 worker 和 /home/ubuntu/director-card-ak-20260917/nginx-before-card-ak.conf。

## 2026-09-17 · 多选启用生成 AK

生成 AK 列表改为复选框，enabled_key_ids 独立保存，勾选不关闭其他 AK。卡片仅列出已启用 AK，模型范围与提交校验一致；停用 AK 禁止新提交，旧任务仍绑定原 AK 查询。旧配置无启用列表时默认全部启用，避免先前混用卡片升级后显示不可用。234 项完整测试、补充停用校验后的 76 项相关测试和 Electron smoke 通过；候选与正式 HTTPS 各 49 项协议检查通过。生产镜像 0.11.8-director-multi-enable，端口 8625；保留 8624 worker，回滚配置 /home/ubuntu/director-multi-enable-20260917/nginx-before-multi-enable.conf。

## 2026-09-17 · AK 行内展开编辑

生成/管理 AK 的查看编辑按钮替换为展开/收起编辑，复用编辑区移动到对应 AK 行下；打开设置默认收起已有配置，添加操作展开新配置。启用操作独立于编辑，保留多选与绑定查询。Electron smoke 通过，调整选择器只统计启用复选框，避免把行内编辑输入计入 AK 数量；候选/正式 HTTPS 各 49 项协议检查通过。生产 0.11.9-director-inline-edit、8626，保留旧 worker 和 /home/ubuntu/director-inline-edit-20260917/nginx-before-inline-edit.conf。

## 2026-09-17 · 管理 AK 同样多选启用

管理 AK 使用与生成 AK 一致的复选框及行内展开/收起编辑，独立 enabled_key_ids。组织汇总查询所有已启用管理 AK，按 organization_id 去重并展示各组织与 Decimal 合计；生成 AK 的绑定费用查询仍固定使用绑定管理 AK，停用绑定项提示先启用。失败查询不输出误导性部分总额。237 项测试与 Electron 多组织汇总验证通过；候选和正式 HTTPS 各 49 项检查通过。生产镜像 0.11.10-director-management-multi，端口 8627；保留 8626 worker，Nginx 回滚配置 /home/ubuntu/director-management-multi-20260917/nginx-before-management-multi.conf。

## 2026-09-17：公开分享访客只读

公开分享链接支持未登录直接读取关联项目、历史、素材与已有评论；每次请求校验链接有效期与撤销状态，禁止跨项目资源访问及所有访客写入。登录后的成员权限保持不变。访客初始化使用外部脚本以符合 CSP，媒体地址附带分享凭证。全量 238 项测试通过，最终初始化调整后 10 项网关测试通过；候选与正式 HTTPS 各 54 项检查通过。最终浏览器连接不可用，未完成修复后的浏览器视觉复验。生产镜像 0.11.11-director-public-view、端口 8628，保留 8627 worker；回滚配置 /home/ubuntu/director-public-view-20260917/nginx-before-public-view.conf。
## 2026-09-16 · Windows 本机升级与 UUID 分片迁移验证

- 安装新版云存储 Python 依赖，pip check 通过。旧工作台及 PostgreSQL 停止后，在忽略的 .local 升级备份目录创建完整冷备份并逐文件 SHA256 校验，再运行数据库初始化与分片迁移。
- 迁移前后逐条比较实体 UUID、JSONB、创建和更新时间，以及账号与密码哈希；全部一致，素材文件保留，UUID 分片归属和 prepared transactions 配置验证通过。源码 Electron 和本机 ComfyUI 已启动，工作台页面、新界面资源及 ComfyUI system_stats 正常。
- 完整回归首次结果为 192 passed、8 skipped、1 failed；唯一失败为 Windows 不支持 POSIX 0600 stat 权限语义。修正测试为在实际配置目录保护流程下验证 Windows ACL，允许当前用户与系统/管理员/所有者权限，拒绝其他主体；POSIX 保持 0600 检查。修正后云存储专项 32 passed。8 项伴随云端环境集成测试跳过，本次未调用付费模型或执行真实云上传。

## 2026-09-18 · 视频剪辑时间轴，桌面与线上一致

- 画布底部新增「时间轴」，支持横向滚轮/滚动条、滑块及 Ctrl/⌘+滚轮缩放、读取真实视频时长后连续追加、片段拖动、边缘裁剪、精确入点/时长、磁吸及 Alt 临时取消磁吸。
- 重叠按后放置/拖动或手动置顶优先，保留下层；金色开始线与红色播放线分开，浏览器合成预览视频及字幕。字幕支持跟随片段的相对偏移和绝对时间；移除视频保留字幕并转为绝对定位。
- 时间轴数据位于 canvas.timeline，共享既有自动保存/CAS、成员权限及云同步。后端限制数量/时长/字幕绑定，复用媒体归属校验；同步新副本重写片段/字幕 ID 与 clip_id，额外记录片段顺序变更，保持覆盖顺序。
- 修复验证发现的零秒开始线遮挡裁剪手柄问题，将开始线拖动命中区域限定在标尺；长时间轴刻度仅渲染可见范围。只读用户可预览及临时缩放。
- 验证：全套 pytest 246 passed（5 条既有依赖弃用警告）；真实 Electron 独立数据库测试通过连续追加、鼠标拖动、磁吸、重叠、左侧裁剪、跟随/绝对字幕、缩放与横向滚轮、保存重开及播放。截图 docs/screenshots/timeline.png。
- 导出 56 个运行时文件到 API 仓库；候选及生产 HTTPS 各 48 项检查通过（临时账号自动清理，含时间轴存储、同步副本字幕重新绑定及成员/只读权限）。生产版本 0.12.0-director-timeline，端口 8629。保留旧 8628 worker；回滚配置 /home/ubuntu/director-timeline-20260918/nginx-before-timeline.conf。核对生产 56 个源文件哈希与客户端一致，并核对公开 timeline.js、timeline.css、studio.js 的前缀改写后内容一致。
- 当前提供编排和浏览器预览，未实现渲染成片导出；切换/定位精度取决于浏览器媒体解码与加载。

## 2026-09-18 · 卡片命名与时间轴来源辨认

- 图片/视频生成卡片与素材卡片增加标题旁「✎」重命名，独立 title 字段避免素材规范化保存覆盖名称。未命名生成卡片以类型加短卡片 ID 区分，评论区继续使用已有设置改名。
- 时间轴和引用素材选择显示卡片名、生成时间、结果序号/短任务 ID；旧片段通过现有媒体引用解析当前卡片名。独立片段名留空跟随来源，设置后保留别名；详情保留来源，轨道显示原视频取用范围。评论选段追加时采用该选段的入点和时长。
- 后端校验命名长度，名称及来源快照随画布保存；卡片改名更新已引入片段的来源快照，不改变原文件及剪辑位置。
- 验证：31 项时间轴/同步/云端网关测试通过；真实 Electron 验证卡片重命名、已有片段更新、独立片段名与来源保留、保存重开及原时间轴交互。线上候选与生产各 49 项协议检查通过。
- 生产版本 0.12.1-director-naming，端口 8630，保留 8629 worker；回滚配置 /home/ubuntu/director-naming-20260918/nginx-before-naming.conf。

## 2026-09-18 · 重叠错层、多时间轴与 PIN 同播

- 视频交叉时按合成优先级自动错层，较高片段起点用竖线连回底层，支持多重交叉及端点接续。时间轴保留原非破坏性覆盖逻辑。
- 同项目最多 8 个独立时间轴，可新增、复制、命名、收起及删除附加轴。沿用 canvas.timeline 兼容旧数据，附加轴保存到 canvas.timelines；复制重新生成片段/字幕 ID，后端验证跨轴唯一性并校验每轴素材权限，同步快照重写所有引用。
- 增加共同播放时钟、同步定位/播放/暂停及独立播放，隐藏轴不参与同步播放；关闭某轴不会停止其余播放器。
- 可移动/缩放的 PIN 对比窗支持双画面、三画面、四宫格，最多四个 PIN。声音支持任一路、全混音和静音。复用同一个 video DOM，避免小预览与 PIN 重复音频。
- 验证：248 项后端回归通过；Node 错层测试覆盖三重交叉、端点接续及顺序变化；真实 Electron 测试覆盖原编辑能力、多轴复制/存储、错层竖线、同一时钟、联动定位/暂停、独立播放、三种音频选择、双/三/四画面、关闭 PIN 回位、隐藏轴及播放中关闭轴。截图由 6 秒绿色测试视频生成，不含用户媒体。
- 发布：0.13.0-director-multi / 8631，候选及正式 HTTPS 各 50 项检查通过，包含附加时间轴同步复制与字幕重新绑定。保留旧 8630 worker；回滚配置 /home/ubuntu/director-multi-20260918/nginx-before-multi.conf。生产 56 个运行时文件哈希及三份公开前端文件与本地一致。

## 2026-09-18 · 修复附加时间轴拖动与默认音源

- 复现第二轴鼠标拖动失败：动态生成的 inspector 控件仍使用第一轴 ID，第二轴找不到删除按钮，pointerdown 在建立拖动状态前异常。为每轴动态控件赋予独立 ID 和统一 data-tl 标识，修复第二轴拖动、裁剪、命名、字幕及移除操作的事件绑定；拖动范围校验不再吞掉后续渲染异常。
- 默认声音改为「跟随播放轴」，独立播放切换到发起轴，同步播放固定在发起轴；仍尊重用户手动固定音源、混音及静音。每路预览显示「音源 / 静音」，PIN 与小预览一致。
- 在原 Electron 测试中增加第二轴实际鼠标拖动（断言不修改第一轴）、裁剪、动态名称输入、自动音源测试。测试素材增加 440 Hz AAC 音轨，验证第二轴解除静音、持续播放且音频实际解码；测试进程使用 mute-audio 避免在用户机器播放测试音，不把操作系统声卡输出当成已验证。完整 Electron 测试及 Node 错层测试通过。
- 拖动/音源修复已部署为 0.13.1-director-timeline-fix / 8632，候选及生产各 50 项检查通过，共用运行时 56 文件与客户端一致；保留旧 8631 worker，回滚 /home/ubuntu/director-timeline-fix-20260918/nginx-before-timeline-fix.conf。

## 2026-09-18 · 数字行列布局

- 将 PIN 固定布局下拉框替换为「行 × 列」数字输入，支持任意 1–16 整数行列组合；新增显示路数计数，网格容量不足时显示前 N 路并保留其他 PIN 选择。新增 PIN 超过容量时按当前列数扩展行数。
- 取消 PIN 四路硬限制，可 PIN 当前项目全部时间轴（项目仍最多 8 条）。原双画面/三画面/四宫格均可通过数字设置。
- Electron 回归验证 2×3 自定义网格、无效数字回退，以及第二轴拖动/裁剪、真实音轨解码、声音路由和原多轴功能。
- 五路 PIN 测试通过，确认不再限制四路。发布 0.13.2-director-grid / 8633，候选及正式 HTTPS 各 50 项检查通过；保留旧 8632 worker，回滚 /home/ubuntu/director-grid-20260918/nginx-before-grid.conf。核对生产 56 文件与客户端一致。

## 2026-09-18 同项目自动更新与编辑占用

- 新增共享数据库编辑租约，每张卡片、每个时间轴独立锁定；服务端在项目保存事务内检查他人占用，返回 423，客户端显示账号并阻止编辑。鼠标／焦点进入申请，离开保存后释放，异常断连 15 秒过期。同账号不同窗口也区分会话。
- 约 1 秒检查租约与项目版本，仅版本改变时拉取画布。三方合并支持不同卡片并行修改及 CAS 重试；冲突保留草稿。按变化更新 DOM，保留本地视角、正在输入的控件与未变化时间轴的播放状态。
- 云端桥接透传客户端标识，编辑成员可申请，viewer 不可申请；更新共享 runtime 与桌面一致。
- 验证：249 项 Python 测试通过；新增 Node 三方合并检查；真实 Electron 双窗口同账号验证互斥、编辑者标记、并行保存、自动更新、输入光标保留、时间轴占用和关闭窗口租约过期。候选服务 HTTP 58 项通过。
- 发布：xialiwei-api:0.14.0-director-live，8634；旧 8633 保留，部署目录 /home/ubuntu/director-live-20260918。

## 2026-09-18 · AK 显示与复制上线

生成与管理 AK 行内编辑新增显示 / 隐藏及复制按钮；仅点击时通过已认证 POST reveal 读取指定配置，响应 no-store，关闭设置或换配置清空。在线版所属账号隔离、同步 AK 拒绝及匿名拒绝已验证。80 项相关 Python 测试通过，候选与正式 HTTPS 各 70 项检查通过，生产四个更新文件 SHA-256 与本地一致。发布 xialiwei-api:0.14.1-director-ak / 8635；保留旧 8634 worker，回滚配置 /home/ubuntu/director-ak-20260918/nginx-before-ak.conf。

## 2026-09-18 · 对话模式与 UUID 链式记录

工作台顶部新增对话模式，按已启用 service-inference AK 查询语言模型，每次提问指定模型并重新校验可用性；同一对话可切换 AK / 模型。普通问答不发送工具参数，GPT-6 默认 Responses，其余默认 Chat Completions，支持手动选择。

对话及每个记录包使用独立新 UUID，分别求余路由实体分片。每包 25 轮问答，对话保留 first_id / last_id，记录包通过 prev_id / next_id 双向链接；包满时原子创建 / 链接新包。完整历史分页读取，回答携带最近 20 轮成功问答；失败或中断问答不进上下文。持久化问题后后台请求，记录回答、模型、AK 名称、时间、服务商用量；请求 UUID 去重，不自动重发。服务切换保留 330 秒有效等待窗口，避免误判另一 worker 正在处理的请求；异常后需核对服务控制台再确认继续。对话账号私有，不纳入项目协作 / 同步。

验证：全量 258 项 Python 测试通过；最终相关 17 项通过；真实 Electron / 隔离 PostgreSQL 验证切换模型、20 轮上下文、不同分片双向记录包、重复提交、失败 / 中断恢复、Markdown 安全过滤、历史重新打开、账号隔离和窄屏布局。模型回答采用模拟，不发起计费问答。候选与正式 HTTPS 各 84 项检查通过，七个发布文件生产 SHA-256 与本地一致。发布 xialiwei-api:0.15.0-director-dialogue / 8636；保留 8635 worker，回滚 /home/ubuntu/director-dialogue-20260918/nginx-before-dialogue.conf。

## 2026-09-18 · 对话文件附件

对话输入区支持添加多个文件、拖入及粘贴图片，上传中禁用发送，发送前可移除。支持 PNG/JPEG/WebP、PDF、DOCX 和文本 / 代码；文本 / DOCX 提取有界文字，图片和 PDF 使用对应 Chat Completions / Responses 原生内容字段。单个 10 MB、每轮 5 个、合计 20 MB；最近 20 轮附件总量同样有界。

附件以独立 UUID 求余存入实体库，原文件保存在账号数据目录 dialogue-files，绑定 owner_id / conversation_id；问答包保存附件元信息 / 引用，成功历史追问继续携带附件，可从历史下载。文本下载强制 attachment / octet-stream，在线响应 no-store / nosniff；对话接口显式走账号作用域，项目头不能用于读其他账号附件。

验证：274 项 Python 全量测试通过，最终相关 33 项通过；真实 Electron / 隔离 PostgreSQL 检查文本实际进入模拟模型输入、图片 / PDF 两种协议字段、切模型后的附件上下文、附件历史 / 下载、UUID 分片、跨账号读写拒绝、跨对话引用拒绝、选择 / 拖入 / 粘贴 / 移除及窄屏。未发起计费模型问答。候选和正式 HTTPS 各 92 项检查通过，七个更新文件生产哈希与本地一致。发布 xialiwei-api:0.15.1-director-files / 8637；保留 8636 worker，回滚 /home/ubuntu/director-files-20260918/nginx-before-files.conf。本地客户端已正常退出并重启加载新接口，原数据保留。

## 2026-09-18 · 对话图片展示、阅读设置与轮次配置

图片附件在发送草稿与聊天历史中显示缩略图，点击 / 键盘开启既有大图查看器，原文件仍可下载。回复字号提供 13 / 15 / 18 / 21 / 24 px，保存本机阅读偏好；每条回答可单独展开 / 收起，刷新正在回答的记录时保留本次折叠状态。

每个对话持久保存 context_turns（0–100，默认 20）与 pack_size（1–100，默认 25）。0 轮上下文仅发送当前问题；最近成功问答按配置跨记录包读取。修改分包容量仅影响后续追加，不重排旧包，仍然每轮立即持久化。配置写入校验整数 / 范围 / 所属账号，回答处理中拒绝修改；旧对话兼容默认值。既有上下文文字与附件体积上限保留。

验证：275 项 Python 测试通过，真实 Electron / 隔离 PostgreSQL 检查图片加载与大图、历史图片重开、24 px 样式、折叠状态、0 轮实际模型输入、1 轮分包及链式链接、非法配置拒绝和窄屏；模型为模拟，未计费。候选与正式 HTTPS 各 97 项检查通过，四个生产文件 SHA-256 与本地一致。发布 xialiwei-api:0.15.2-director-reading / 8638；保留 8637 worker，回滚 /home/ubuntu/director-reading-20260918/nginx-before-reading.conf。本地客户端正常退出并重启，新服务 ready http://127.0.0.1:59311。

## 2026-09-19 · 对话本次上下文检查

每轮记录固化 `context_ids` 与提交统计：历史轮数、消息数、文本字符、附件数量 / 原文件字节以及发送给 service-inference 的 JSON 请求体 UTF-8 字节数（包含图片 / PDF 的传输编码）。回答下方显示摘要，按需读取同账号同对话的链式记录包并展开当次实际携带的历史问题、回答和附件名称；当前问题不重复计入历史列表。后续修改上下文轮次不会改变旧请求记录，旧问答无统计字段时保持兼容。

验证：276 项 Python 测试通过；真实 Electron / 隔离 PostgreSQL 验证摘要、展开内容、当前问题排除及跨分包读取。候选与正式 HTTPS 各 100 项检查通过，四个生产文件 SHA-256 与本地一致。发布 `xialiwei-api:0.15.3-director-context` / 8639，保留 8638 worker；回滚 `/home/ubuntu/director-context-20260919/nginx-before-context.conf`。本地客户端已重启。

## 2026-09-19 · 对话全屏阅读布局

对话窗口改为占满 `100vw × 100dvh`，去除四周留白和圆角；标题、状态和输入区压缩，聊天记录使用 flex 占据全部剩余高度。窄屏下对话 / 模型 / 接口与阅读配置改为单行横向滚动，侧栏缩至 58px，帮助说明隐藏，避免设置和输入框挤占回答阅读区。

真实 Electron 验证桌面记录区不低于 300px、640×860 窄屏不低于 220px，窗口边界与视口一致且无横向溢出；候选和正式 HTTPS 各 100 项检查通过。发布 `xialiwei-api:0.15.4-director-fullscreen` / 8640，保留 8639 worker，回滚 `/home/ubuntu/director-fullscreen-20260919/nginx-before-fullscreen.conf`。

## 2026-09-19 · 对话分类、资料信息与归档

对话增加账号私有的分类实体，分类和对话分别使用独立 UUID 求余路由。侧栏支持创建和重命名分类，按分类显示对话；每个对话可编辑 120 字标题、1000 字描述及所属分类。归档状态持久化，普通列表隐藏归档对话，可切换归档列表并恢复。首轮问题仅在标题仍为“新对话”时自动命名，保留用户预先设置的标题。

Markdown 回答表格使用折叠的 1px 细边框、表头底色及单元格留白，宽表格在回答区横向滚动。验证：277 项 Python 测试通过；真实 Electron / 隔离 PostgreSQL验证分类新建 / 改名、资料保存、描述列表、归档 / 恢复、历史重开、表格计算样式和窄屏。候选与正式 HTTPS 各 105 项检查通过，五个生产文件 SHA-256 与本地一致。发布 `xialiwei-api:0.16.0-director-library` / 8641，保留 8640 worker，回滚 `/home/ubuntu/director-library-20260919/nginx-before-library.conf`。本地客户端已重启。

## 2026-09-19 · 文档式对话资料与可搜索模型选择

对话标题和描述改为常驻两行的 `contenteditable` 行内编辑，无输入框外观；限制分别为 120 / 1000 字，粘贴转为纯文本，输入停止 700ms 或失焦后自动保存，仍保留手动保存。资料更新使用 PostgreSQL JSONB 原子合并，避免模型回答完成时互相覆盖 pending、记录包和最近模型字段。分类位于标题右侧，通过 datalist 点击候选或键入搜索；同账号分类名称唯一。

AK 与模型改为可搜索 datalist。每次成功持久化请求同时保存 `last_credential_id`、`last_model` 和 `last_protocol`，重开对话恢复仍存在且启用的选择；失效 AK / 模型清空并提示重新选择。新对话在本机沿用最近选择，首次使用选择第一个可用组合。

验证：277 项 Python 全量测试、26 项对话相关测试通过；真实 Electron / 隔离 PostgreSQL验证两行资料、contenteditable、自动 / 手动资料持久化、分类搜索、AK / 模型搜索、重开恢复、附件与跨包历史、桌面和窄屏阅读高度。候选与正式 HTTPS 各 107 项检查通过。发布 `0.16.1-director-editor` / 8642 后以原子资料合并修订为 `0.16.2-director-metadata` / 8643；保留 8642 worker，回滚 `/home/ubuntu/director-metadata-20260919/nginx-before-metadata.conf`。

## 2026-09-19 · 对话资料即时保存与折叠设置

移除标题 / 描述右侧的“保存信息”按钮；contenteditable 输入停止 250ms 或失焦即自动保存，分类变更立即保存，右侧仅保留归档操作。回复字号、历史轮次、每包轮次及保存配置收进“对话设置”，默认折叠并通过按钮展开 / 收起，释放回答阅读空间；入口与 AK、模型、接口操作保持在同一行。 用户提问显示为靠右气泡，宽度随内容伸缩，设置最小宽度并限制最大宽度。 回答操作缩为“折叠 / 展开”和复制图标；折叠后保留 32px 回答高度，便于滚动定位。 标题 / 描述标签仅在对应内容进入编辑焦点时显示，失焦后收起以压缩资料区。 折叠回答改为 180–420px 卡片并在卡片内滚动；整体对话字号使用五节点滑杆（较小 / 小 / 中 / 大 / 较大），兼容既有字号偏好。 资料区平时压为单行，标题旁内联描述；进入标题 / 描述编辑焦点才展开两行，失焦后恢复。 设置可调整折叠卡片默认最小宽高；每条卡片支持双向拖动，按对话 UUID 与回答 ID 在本机持久保存独立尺寸。 折叠卡片使用完整可见边框；Markdown 表格由独立容器横向滚动，回答卡片不再整体横移。 展开回答清除单条卡片宽高并恢复占满阅读区，保存尺寸仅在折叠时应用。 单条卡片拖拽结束或尺寸稳定 250ms 后，以“对话 UUID + 回答 ID”自动保存并提示，不影响其他回答。 “本次提交”折叠摘要使用 fit-content，展开上下文恢复全宽。 每条回答增加 Aa 字号图标，独立字号及折叠状态均按“对话 UUID + 回答 ID”持久化。 点击 Aa 弹出五节点字号滑条，拖动即时预览并保存当前回答档位。 Markdown 回答含标题时生成可折叠层级目录，点击定位标题；目录展开 / 收起状态按回答持久化。

真实 Electron 验证默认折叠、展开 / 收起、自动保存及侧栏标题 / 描述同步；候选与正式 HTTPS 各 107 项检查通过。先发布自动保存与紧凑阅读系列版本，最终发布折叠目录满高内滚动的 `xialiwei-api:0.16.18-director-toc-inner-scroll` / 8658；保留 8657 worker，回滚 `/home/ubuntu/director-toc-inner-scroll-20260919/nginx-before-toc-inner-scroll.conf`。标题目录与回答正文改为左右分栏，折叠卡片中各自滚动，窄屏回退为上下布局，不再覆盖正文。目录收起时缩为 fit-content 图标列，展开后恢复目录宽度；目录使用无编号导航链接，不额外生成与标题原文重复的列表序号。点击目录项时将目标标题滚动到正文可视区域纵向中部，并短暂高亮目标，避免停在底部造成无响应错觉。回答元信息与卡片增加 9px 间距。对话设置改为锚定按钮的浮层，点击外部或 Escape 关闭，窄屏从底部显示；标题与描述自动保存不再切换全局 busy 或抢走 contenteditable 焦点。提问区改为组合输入框，textarea、附件缩略图与放大的上传按钮共用一个容器；附件以小卡片显示，并在右上角提供圆形 × 删除标。折叠回答中展开的标题目录撑满卡片可用高度，目录列表与右侧正文分别在各自区域内纵向滚动。根目录 `SKILL.md` 增加 Antigravity 调用流程，并提供不会输出 AK 的 service-inference CLI，用于列举配置/模型、调用文本模型以及查询实际用量、费用与余额。

## 2026-09-19 · Electron 代理模式与命令工具

对话控制栏新增“对话模式 / 代理模式”。代理模式仅在 Electron 客户端启用并强制使用 Responses API；模型通过严格的 `run_command` function tool 返回参数数组、工作区相对目录和执行原因，不从 Markdown 猜测命令。每次命令先显示持久化审批卡片，用户允许后才由 Electron 主进程以 `shell:false` 执行，渲染进程不接触 Node API。执行目录限制在 Hotpoor Director 工作区内，超时 120 秒，stdout / stderr 各限制 1 MiB，结果、退出码、信号、耗时与拒绝状态随问答记录保存并回传模型继续回答。在线浏览器会提示代理模式仅限客户端。

验证：279 项 Python 全量测试通过，新增 Responses 工具定义、函数参数校验与续接载荷单测；真实 Electron 对话回归通过，覆盖模型切换、上下文、附件、历史重开、Markdown、折叠目录和窄屏。测试模型为本地模拟，没有发起计费问答。

已上线 `xialiwei-api:0.17.0-director-agent` / 8659；候选和正式 HTTPS 各 107 项检查通过，保留旧 8658 worker。回滚配置：`/home/ubuntu/director-agent-20260919/nginx-before-agent.conf`。
代理模式的“本次提交”请求字节数包含 Responses 工具定义，保持与实际 JSON 载荷一致。修订发布 `xialiwei-api:0.17.1-director-agent-stats` / 8660；候选与正式 HTTPS 各 107 项检查通过，回滚 `/home/ubuntu/director-agent-stats-20260919/nginx-before-agent-stats.conf`。

## 2026-09-19 · 修复代理工具提交与执行目录侧栏

定位首个代理请求 400：service-inference 的严格 function schema 要求 `required` 覆盖全部 properties，补入 `cwd` 后使用真实 `gpt-6-astra` 验证成功返回 `run_command`，并验证 `previous_response_id + function_call_output` 可继续生成最终回答。原失败轮未生成命令，也没有本地执行。

对话控制栏新增“执行目录”右侧抽屉，通过系统目录选择器添加白名单，可查看及移除；权限写入本机 0600 配置。默认允许 `~/Sites`。模型请求携带当前白名单以生成绝对 cwd，Electron 对 cwd 的 realpath 再校验必须位于任一允许目录，阻止 `..` 或符号链接越界；每条命令仍需单独确认。

验证：280 项 Python 测试通过；真实 Electron 对话回归通过；真实 service-inference 工具申请与结果续接通过。

上线 `xialiwei-api:0.17.2-director-agent-folders` / 8661；候选与正式 HTTPS 各 107 项检查通过，保留旧 8660 worker。回滚 `/home/ubuntu/director-agent-folders-20260919/nginx-before-agent-folders.conf`。

## 2026-09-19 · 本机凭据管理

新增凭据侧栏，按名称添加、覆盖更新、删除密码。Electron safeStorage 使用操作系统安全存储保护加密密钥，密码密文写入本机0600文件；不提供解密读取IPC。代理请求只携带凭据名称，run_command 支持 env NAME={{credential:name}} 前缀，由主进程消费并注入环境变量；使用前显示原生命令及凭据确认，结果对原样密码脱敏。此能力不构成对任意命令的沙箱隔离。测试覆盖持久化、更新删除、变量注入及脱敏，并完成真实 Electron OS 加密往返验证。

## 2026-09-19 · 命令输出、停止与工具续接

增加凭据输入显示/隐藏按钮；命令卡片通过 IPC 实时展示 stdout/stderr，分段 UTF-8 解码并在主进程处理跨分段凭据脱敏，保留内部滚动位置。增加停止按钮，macOS 向命令进程组发送 SIGINT，超时强制结束，保留输出。统一恢复对话列表禁用状态。

确认服务上游 previous_response_id not found 导致工具结果回传 400；新轮次保存完整工具上下文（含加密推理项）进行无状态续接，限制并行工具调用。真实模型续接验证通过，测试未运行本地命令。失败/中断轮次和命令结果纳入后续历史，长输出标注截断，参数校验明确指出字段。

验证：16 项 dialogue Python 测试，命令流输出/脱敏、真实 SIGINT、控件恢复、轮询与防重复执行 Node 测试通过。同步 director_runtime；本次代码推送不代表服务器部署。

## 2026-09-19 · README 与助手 SKILL 更新

README 首页补入完整对话/代理指南，保留画布、时间轴、云端协作与部署说明。新增四张当前 Electron 截图：对话目录与表格、设置浮窗、命令确认、实时输出；独立测试数据库、模型与执行桥均为演示数据，无私人资料或真实命令执行。截图脚本、来源说明和 SHA-256 清单一起保存。

SKILL 补入失败历史、无状态工具续接、命令停止平台差异、cwd 权限边界、本机加密凭据和截图复现说明。文档本地链接检查、skill-creator 格式验证及16项对话测试通过。

## 2026-09-19 · 单对话30天完全访问与凭据中文描述

新增完全访问授权按钮，用户原生确认后按对话保存30天授权；倒计时3秒自动执行，可取消本次，切换/关闭对话取消倒计时，支持撤销与到期恢复。主进程再次校验自动执行资格；允许免重复确认使用授权时已有的凭据，新凭据仍单独询问，cwd规则保持。没有替用户启用授权。

凭据支持中文描述及仅修改描述；兼容旧密文格式，密码不回显。模型接收名称与用途描述，不接收密码值。验证覆盖授权隔离、持久化、到期、撤销、描述迁移、倒计时不因重绘重置、取消回到手动；16项对话测试通过。

## 2026-09-19 · 完全访问改为永久授权

按用户要求取消30天有效期，当前对话授权永久有效，直到手动撤销。旧的仍有效授权自动迁移为永久；已撤销或已过期的授权不重新激活。保留3秒倒计时、单次取消、停止、已有凭据范围与工作目录校验。永久持久化、旧格式迁移、撤销及倒计时测试通过。

### 2026-09-19 · 对话上下文自动分段总结

- 修复旧历史文字/附件累计超过 120000 字符后无法继续提问的问题：新输入单独校验，历史在后台接近 80000 字符时先总结再发送。
- 历史按最多 32000 字符分段，各段独立总结；每四份摘要合并一次，必要时多层合并。分段和合并结果分别保存为 `dialogue_summary` entity，记录归属、来源记录包/轮次、父摘要、模型和用量；内容指纹可复用已经完成的分段。
- 对话保存总摘要与精确历史轮次边界；下一轮携带总摘要和边界之后的新记录，原始问答、附件、命令记录不改写。再次达到阈值时合并旧摘要与新记录。历史轮次设置为 0 时不携带摘要。
- 代理续接也会先总结历史及刚执行的命令结果，再开启新的 Responses 上下文，避免悬空工具调用和继续携带旧 response ID；总结请求不开放工具。
- 界面显示分段/合并进度，在「本次提交」中查看实际携带的总摘要。总结失败保留原始历史及已完成分段，并解除本轮等待状态；下一次提交可复用已完成分段。
- 验证：新增模拟服务回归覆盖两种接口、15 万字符旧历史、分段中断复用、代理续接、附件限制和重复总结；全套 pytest 291 项通过。未调用真实付费模型。
